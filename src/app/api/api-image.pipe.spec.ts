import { ApiImagePipe } from './api-image.pipe';
import { ApiService } from 'app/api/api.service';

describe('ApiImagePipe', () => {
    it('create an instance', () => {
        // const pipe = new ApiImagePipe();
        // expect(pipe).toBeTruthy();
    });
});
